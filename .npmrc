# 依存パッケージの install スクリプトは、package.json の allowScripts に書いたものだけ実行する。
# 未承認のものは警告ではなくエラーにして、install を止める（サプライチェーン攻撃の典型的な侵入口）。
# 新しい依存を足して落ちたら、中身を見てから npm approve-scripts / npm deny-scripts で明示する。
strict-allow-scripts=true
